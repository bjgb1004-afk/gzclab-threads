# 스레드 계정 전략 검토 (2026-09-24)

마케팅 관점으로 queue.json / gen_district_posts.py / publish.py / publish.yml 점검.

## 고친 것

- [x] 훅 문구가 사실과 어긋남 — `first >= second*3` 하나뿐이라 16회 대 7회(2.3배)도
      "생각보다 촘촘함"으로 나감. 69개 중 48개가 같은 문장. 구간을 5단계로 나눔.
- [x] 마무리에 댓글 유도 문장 3종 로테이션 추가. "댓글에 구 이름 남겨줘"는 댓글과
      다음 글감을 동시에 받는 자리.
- [x] 상호명 깨짐 — 원본 데이터에 괄호 열린 채 잘린 상호 83곳(`훼미리마트(대림중앙점`).
      괄호 앞까지만 출력.
- [x] 링크 빈도 74% → 39%. 댓글 링크도 매일이면 광고 계정으로 읽힘.
- [x] 댓글이 링크만 달리던 것 → 멘트 1줄 + 링크, 멘트 4종 로테이션.
- [x] Play 링크에 `referrer=utm_source%3Dthreads...` 부착. Play Console 유입 리포트에서
      설치 기여 확인 가능.
- [x] 약한 구 기준 상향(1위 1등 2회 → 3회). 수정구 제외.
- [x] id에 시도 포함(`top5-서울-중구`). '중구'는 6개 시도에 있어 다른 시도 돌릴 때
      중복으로 걸러지던 버그.
- [x] 슬롯 글 소진 시 fallback이 큐 맨 앞(=TOP5)을 집어서 하루 3번 TOP5가 나가던 문제 →
      직전 발행과 다른 종류를 집도록 변경.
- [x] 저녁 슬롯 19:07 → 21:07 (KST). 2주 보고 반응 안 오르면 되돌린다.

## 남은 것 (결정 필요)

- [x] **pm/night 큐 고갈** → (a) 채택. pm/night 각 20편씩 총 40편 추가(talk-pm-11~30,
      talk-night-11~30). pm 14→34, night 14→34로 34일 치 확보. 기존 night 링크 2건도
      referrer 누락돼 있던 것 `utm_campaign=talk`로 맞춤(잡담글은 top5와 구분해서 집계
      가능하게).
- [x] **성과 측정 부재** → `collect_insights.py` 추가. 발행 24시간 지난 글만, 이미
      `insights` 키 있으면 건너뛰고 딱 한 번만 조회(재측정 안 함 — 필요해지면 그때 추가).
      `.github/workflows/insights.yml`로 하루 1회(KST 00:30) 실행, queue.json에 커밋.
      GitHub Actions: 이 repo는 public repo라 무료 분 제한 자체가 없음(퍼블릭은 무제한).
      Threads API 호출은 하루 최대 3건(발행 3건/일)뿐이라 Meta 기본 한도(시간당
      200콜/유저) 대비 여유 큼 — 소진 걱정 없음.
      self-check: `test_collect_insights.py`.

## 되돌린 것 (2026-09-26)

- [x] 계정소개 3부작(intro-1~3)을 큐 맨 앞에 넣었다가 **철회**. 이유: 계정이 이미
      로또 지역 TOP5 시리즈로 8건 돌아간 상태라 지금 와서 "이 계정 소개" 글을 넣으면
      흐름이 끊긴다. 도달 검증된 시리즈(2,676뷰)의 슬롯 3개를 데이터 없는 자기소개로
      쓰는 건 손해. 오래된 메모리에 "발송 대기"로 남아 있었다는 이유만으로 되살릴
      근거가 못 된다. 큐는 원상복구(pending 132, 맨 앞 top5-경기-평택시).
